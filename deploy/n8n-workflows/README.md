# n8n workflow exports

Codex will implement `automation_specs/*.yaml`, test each workflow with synthetic data, export JSON here, remove credential material, and keep workflows disabled until validation passes.
