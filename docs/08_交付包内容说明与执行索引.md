# 当前工程内容说明与执行索引

**最后校准：2026-09-05**

> 本文件不再描述 2026-07 的“初始交付压缩包”。当前项目已经是持续演进的 Governance + Commerce Runtime + Real SKU 工程。

## 1. 这个仓库现在解决什么问题

`Jovifei/Automation_Seal` / `E:\project\jovi-automation` 负责保存：
- 项目当前路线；
- Human Decision / Gate；
- audit mirror；
- Product/Commerce specs；
- OSS 采用边界；
- cloud reference / verifier；
- 本地 Codex/Independent Auditor handoff；
- C4 Pilot governance。

真正 Commerce Runtime 位于独立本地仓：
`E:\project\jovi-medusa-commerce-v1`。

第一真实产品位于：
`E:\project\jovi-modbus-diagnostic-toolkit-v1`。

## 2. 新 Agent 的当前入口

| 文件 | 用途 |
|---|---|
| `docs/CURRENT_PROJECT_GUIDE.md` | 当前目标、架构、阶段、OSS、边界 |
| `docs/HISTORICAL_DOCUMENT_STATUS.md` | 哪些是历史、哪些仍有效 |
| `README_FIRST.md` | 仓库首次阅读入口 |
| `PROJECT_STATE.json` | 机器可读当前状态 |
| `NEXT_STEP_MAP.md` | 当前阶段到下一阶段 |
| `STATUS.md` | 时间序列执行记录 |
| `docs/commerce/README.md` | Commerce 专题 current/completed/history 索引 |
| `AGENTS.md` | Agent 长期行为规则 |
| `CODEX_MASTER_TASK.md` | 当前执行主任务 |

## 3. 已完成里程碑在哪里

### Medusa / R6 / R2-R3
见 `docs/commerce/MEDUSA_*` 与相关 audit/result。它们属于 completed-stage evidence。

### C2
- `docs/commerce/C2_CLOUD_REFERENCE_*`
- `reference/commerce/c2/**`
- `docs/commerce/C2_LOCAL_AUDIT_CLOSURE_MIRROR_20260905.md`

### C3
- `docs/commerce/C3_*`
- `reference/commerce/c3/**`
- `docs/commerce/C3_LOCAL_AUDIT_CLOSURE_MIRROR_20260905.md`
- `docs/commerce/JOVI_RUNTIME_C3_PROMOTION_DECISION_V1.md`

本地 Runtime 原始 evidence 仍优先于这些 mirror。

## 4. 当前 C4 文件

当前重点：
- `docs/commerce/C4_HUMAN_PILOT_PLAN_V1.md`
- `docs/commerce/C4_PILOT_PRIVACY_MINIMIZATION_V1.md`
- `docs/commerce/C4_HUMAN_PILOT_DECISION_CANDIDATE_V1.md`
- `docs/commerce/C4_PILOT_OPERATIONAL_KIT_V1.md`

Decision Candidate 当前仍 `issued_from_human=false`。

Operational Kit 是 **draft/pre-publish material**，最终发布前必须从本地 C3 claim evidence 做逐条 QA，不能直接复制旧文案上线。

## 5. 当前工程关系

```text
Automation_Seal (Governance)
      |
      v
jovi-medusa-commerce-v1 (Commerce Runtime)
      |
      v
jovi-modbus-diagnostic-toolkit-v1 (Real SKU)
      |
      v
C4 Human Pilot (Jovi manual platform actions)
```

`jovi-commerce-engine-v1` 是 legacy archive；`xianyu-auto-reply` 是独立外部适配器，不是当前 Runtime 主线。

## 6. 历史文件怎么用

- `docs/openspec/changes/archive/**`：历史变更证据；
- `docs/superpowers/**`：历史设计/计划/报告；
- 旧 `MEDUSA_*AUDIT*`：阶段审计原件；
- 旧 Track P/I / X0-X4 Prompt：历史执行记录；
- DOCX：旧人类导出，不与最新 Markdown 自动同步。

不要移动/重写历史受管文件；详见 `docs/HISTORICAL_DOCUMENT_STATUS.md`。

## 7. 当前唯一下一动作链

```text
C4 Pre-Publish QA
-> Jovi C4 Human Pilot Decision
-> 5–10 单或固定窗口人工 Pilot
-> C4 recap
-> C4_HUMAN_PILOT_PASS_PENDING_PERMISSION_DECISION
-> Jovi 逐动作 Permission Expansion Decision
```

## 8. 新 Agent 不应该再做

除非现场 SHA/证据发生冲突，否则不要：
- 重做 Phase 0/A/X0；
- 重启 Track P/I；
- 重做 Medusa adoption；
- 重写 C2/C3；
- 切回纯 Python Commerce；
- 为 C4 提前建设 Storefront/S3/n8n production；
- 自动操作闲鱼。

## 9. 当前验证原则

每次接手先做：
1. Git remote/head/status；
2. Governance PR/CI；
3. Runtime original audit/sidecar；
4. Product HEAD/artifact SHA；
5. six real-action flags；
6. 只在证据一致后继续当前阶段。

这样新 Agent 不需要旧聊天，也不会被历史文档拉回已经结束的路线。
