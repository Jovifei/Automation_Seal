# Jovi Automation 当前交付与操作总说明

**最后校准：2026-09-05**  
**当前状态：`C4_HUMAN_PILOT_DECISION`**

> 本文件沿用历史文件名以保持旧引用可用，但内容已更新为当前 Commerce V1 主线。旧同名 DOCX 仅是早期导出，不再代表当前路线。

## 1. 项目现在是什么

Jovi Automation 已从“交给 Codex 的初始治理包”演进为一套可审计的数字产品 Commerce 系统：

- `jovi-automation`：治理、Decision、Audit mirror、Specs、Cloud reference；
- `jovi-medusa-commerce-v1`：正式 Medusa Commerce Runtime；
- `jovi-modbus-diagnostic-toolkit-v1`：第一真实 SKU 产品源；
- `xianyu-auto-reply`：独立外部适配器，当前真实平台动作仍人工。

当前不再执行旧的 `Phase 0/A/X0`、`Track P / Track I` 作为主路线；这些仅属于历史 Governance / Alpha 阶段。

## 2. 已经完成什么

### Medusa / Runtime
- Medusa v2.19.0 已通过独立技术验证并正式受控采用；
- R6 Post-Import Audit PASS；
- Admin session/cookie 的 loopback 问题已在 R2-R3 关闭；
- Playwright、Gitleaks、Syft、Redis replay/recovery 等已验证。

### C2 — Synthetic Digital Commerce E2E
- DigitalRelease / DeliveryAsset / deterministic package / DownloadGrant；
- cross-language ZIP Oracle；
- replay / recovery / concurrency / fail-closed negative tests；
- `C2_INDEPENDENT_AUDIT_PASS`。

### C3 — 第一真实 SKU
- Modbus 产品仓只读资格化；
- 产品测试在 sandbox 执行；
- installer / portable ZIP 原始 SHA 绑定；
- Real SKU + Synthetic Order/Payment 完整 E2E；
- Entitlement / DeliveryReceipt / DownloadGrant；
- 25 个 fail-closed negative cases；
- `C3_REAL_SKU_STAGING_INDEPENDENT_AUDIT_PASS`；
- Runtime audited C3 closure 已经 Jovi Human Decision 提升到本地 `main`；
- `C3_RUNTIME_PROMOTION_AUDIT_PASS`。

## 3. 当前第一真实商品

**Modbus RTU Diagnostic Toolkit**

当前 reported 版本：`0.2.0-dev`。

关键 reported 构件：

- installer：`JoviModbusDiagnosticToolkit-0.2.0-dev-unsigned.exe`
- installer SHA256：`d86ccc3136bc2ed201622c5f961738e9e81762e74e71ac5772ea6d4b5a408e02`
- portable ZIP SHA256：`7525e4c8d4fd55900d46c51e075b92e47d61c7d8e1393383e2e92206855a9628`
- Commerce delivery package SHA256：`4bd5703ae80fcea9c1dcf7d5d1ea2a02fe282a5cf6ef3f04a2c9703db5188e59`

这些是 governance mirror 中的 reported anchors。开始真实 Pilot 前仍需从本地产品/Runtime evidence 重新复核。

## 4. 当前唯一主线：C4 Human Pilot

当前已经有：

`docs/commerce/C4_HUMAN_PILOT_DECISION_CANDIDATE_V1.md`

但它仍是：

`issued_from_human=false`

因此当前允许做的是 **C4 Pre-Publish QA**，不是直接真实发布。

### Pre-Publish QA 必须完成

1. 用本地 C3 `C3_LISTING_CLAIM_EVIDENCE.json` 审核商品文案；
2. 删除或显式标记 Pilot ledger 的 synthetic/example 行；
3. 修正 CRC / SHA256 / 兼容性 / 源码交付等不精确文案；
4. 重新核验当前闲鱼数字/虚拟商品与退款规则；
5. 明确这是 `0.2.0-dev` + unsigned beta Pilot，或由 Jovi 决定先做稳定版；
6. 选择人工交付通道；
7. 核对 Runtime dedicated Git remote；
8. 将清理后的 C3/C4 governance PR 合并到 main。

## 5. C4 的人工控制矩阵

首轮 Pilot 计划 5–10 单或固定时间窗。

| 动作 | 当前执行者 |
|---|---|
| 闲鱼发布 | Jovi 手工 |
| 买家沟通/承诺 | Jovi 手工 |
| 改价 | Jovi 手工 |
| 付款确认 | Jovi 手工 |
| 最终发送交付 | Jovi 手工 |
| 退款/争议 | Jovi 手工 |
| listing/package/evidence 候选 | 系统 |
| Entitlement/Receipt 准备 | 系统 |

C4 的目标是商业验证和人工流程验证，不是无人值守上线。

## 6. 当前隐私边界

Runtime 不保存：
- 闲鱼 Cookie / Token / Browser Profile；
- 买家真实姓名、手机号、地址；
- 完整私聊；
- 原始支付凭证。

Pilot 使用内部 `pilot_order_id` 和最小化脱敏引用。

## 7. 当前 OSS 主线

- Medusa v2.19.0：正式 Commerce Core；
- Playwright：真实浏览器验收；
- Gitleaks v8.24.0：secret scan；
- Syft v1.20.0：SBOM；
- MakePay digital-downloads：仅借鉴 DigitalRelease/private asset/DownloadGrant/idempotent delivery 模式；
- PyInstaller / Inno Setup：只参考现有产品打包配方，不在 C4 前升级；
- n8n production、Trivy、SLSA/cosign 等均不阻塞第一批 Pilot。

## 8. 当前完成定义

技术 C0–C3 已基本闭环。

商业 V1 只有在 C4 完成后才成立：

- 真实 5–10 单或固定窗口；
- 0 duplicate Entitlement/Receipt；
- 0 wrong-version delivery；
- 0 unauthorized platform action；
- package/release/payment fact 可追溯；
- 人工负担/售后/退款原因可量化。

C4 结束状态：

`C4_HUMAN_PILOT_PASS_PENDING_PERMISSION_DECISION`

之后仍需新的 Human Decision 才能扩大任何自动化权限。
